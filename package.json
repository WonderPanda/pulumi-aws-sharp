{
    "name": "aws-sharp",
    "devDependencies": {
        "@types/express": "^4.16.0",
        "@types/node": "latest",
        "@types/sharp": "^0.17.10"
    },
    "scripts": {
        "build-lambda": "rm -rf node_modules/sharp && docker run -v $PWD:/var/task lambci/lambda:build-nodejs8.10 npm install"
    },
    "dependencies": {
        "@pulumi/aws": "latest",
        "@pulumi/cloud": "^0.15.1",
        "@pulumi/cloud-aws": "^0.15.1",
        "@pulumi/pulumi": "latest",
        "axios": "^0.18.0",
        "express": "^4.16.3",
        "sharp": "^0.21.0"
    }
}
